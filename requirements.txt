# =============================================================================
# Algohns V12 — LEAN requirements (Streamlit Community Cloud–safe)
# Only packages that install reliably on a 1 GB Cloud builder. Every heavy or
# build-fragile extra lives in requirements-full.txt; the platform degrades
# gracefully without them (each page shows what to `pip install`).
# Install:  pip install -r requirements.txt
# Full power (local / Docker):  pip install -r requirements-full.txt
# =============================================================================

# --- Core platform & dashboard ----------------------------------------------
streamlit>=1.36            # multipage dashboard (st.navigation API)
pandas>=2.0
numpy>=1.24
scipy>=1.11                # precise bond-yield root-finding (brentq)
python-dotenv>=1.0
requests>=2.31
pyarrow>=14.0              # parquet disk cache

# --- Market data -------------------------------------------------------------
yfinance>=0.2.40           # prices & fundamentals
pandas-datareader>=0.10    # Stooq long history (backtests back to the 1990s)

# --- Module 1: European bond screener ---------------------------------------
beautifulsoup4>=4.12       # parse Borsa Italiana MOT/EuroMOT list pages
lxml>=5.0
#  (bond math is pure NumPy/SciPy; QuantLib cross-check is in requirements-full)

# --- Module 2: Alpaca paper trading -----------------------------------------
alpaca-py>=0.21            # official Alpaca SDK (paper)
#  (Celery/Redis background workers are in requirements-full)

# --- Module 3: universe + optimization --------------------------------------
financedatabase>=2.2       # 300k+ instrument universe (bundled, offline)
#  (PyPortfolioOpt/cvxpy in requirements-full; NumPy optimizer fallback built in)

# --- Module 4: supply-chain graph -------------------------------------------
networkx>=3.2              # directed graph + contagion metrics
pyvis>=0.3.2               # interactive HTML graph
sec-edgar-downloader>=5.0  # download 10-K/10-Q (EDGAR REST fallback built in)
#  (spaCy NER is in requirements-full; RegEx extraction works without it)

# --- Module 5: SEC financial statements -------------------------------------
#  Uses only requests + pandas against data.sec.gov XBRL company facts.
