# =============================================================================
# Algohns V12 — FULL requirements (local / Docker — every feature enabled)
# Includes the lean set plus the heavy / build-fragile extras that may not
# install on a constrained Streamlit Community Cloud builder.
# Install:  pip install -r requirements-full.txt
# =============================================================================
-r requirements.txt

# --- Module 1 extras ---------------------------------------------------------
QuantLib>=1.33             # independent cross-check of YTM/duration/convexity
# borsa-italiana-scraping is GPL-3.0 and NOT on PyPI; install manually if you
# want its richer per-bond detail:
#   pip install git+https://github.com/Librefolio/borsaItaliana-scraping.git

# --- Module 2 extras (background auto-trading) -------------------------------
celery[redis]>=5.3         # async task queue
redis>=5.0                 # broker / result backend
APScheduler>=3.10          # broker-less scheduler fallback

# --- Module 3 extras (convex optimization + extended stats) -----------------
PyPortfolioOpt>=1.5.5      # Max Sharpe / Min-Var / Black-Litterman
cvxpy>=1.4                 # convex solver used by PyPortfolioOpt
ffn>=1.0                   # extended performance stats (cross-check)
# vectorbt>=0.26           # optional vectorised backtesting (heavy)
# backtrader>=1.9          # optional event-driven engine

# --- Module 4 extras (NER) ---------------------------------------------------
spacy>=3.7                 # Named-Entity Recognition (ORG extraction)
#  after install:  python -m spacy download en_core_web_sm
